### app-lab - for development

Imports the AppLab component "wrapping" it as a traditional FE react application.

### app-lab-desktop

Imports the AppLab component in a WAILs FE package, bundled together with the complete FE-BE application.

### cloud-editor-standalone

Imports the CloudEditor component "wrapping" it as a traditional FE react application.

### detachable-monitor - for development

Imports the SerialMonitor component "wrapping" it as a traditional FE react application.
