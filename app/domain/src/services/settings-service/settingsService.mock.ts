import { NetworkItem } from '@cloud-editor-mono/ui-components/lib/components-by-app/app-lab';

export const mockGetNetworkList = (): NetworkItem[] => {
  // Mock network list, could be anything
  return [];
};
