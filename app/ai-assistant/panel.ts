// Lazy entry: the panel UI, code-split via React.lazy (separate from the eager service root).
export type { AgentSidePanelProps } from './src/features';
export {
  AgentSettings,
  AgentSidePanel,
  AiAssistantPanel,
} from './src/features';
