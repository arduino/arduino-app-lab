import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  openExample: {
    id: 'appLabExamples.openExample',
    defaultMessage: 'Open Example',
    description: 'Tooltip on the row arrow that opens an example',
  },
});
